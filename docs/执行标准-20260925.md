# ayh-mj 执行标准（2026-09-25 定稿 · 唯一真相源）

> **本文件是项目唯一执行标准（Single Source of Truth）**——一切出片以此为准。
> 老管线已全部移入 `_deprecated_20260925/`（勿用，见第四节）。
> 记忆口诀：**四池选角 → 六段式提示词 → 明快档 → 新青年体无标点不加粗 → 抖音BGM按剧情**。

---

## 一、出片全流程（现行 7 步）

| 步骤 | 命令/操作 | 说明 |
|---|---|---|
| 1 组合选择 | `python tools/pick_combo.py`（`--commit` 记录） | 四池智能组合：角色组/卖点/角度/片型，保新优先 |
| 2 写 spec | 参照 **`tools/prep_w1_magic.py`**（最新样板） | 官方 Ref2VA 六段式 prompt（见 2.4） |
| 3 校验 | `node tools/check_dialogue.mjs docs/onetake_check_*.txt` | **必须 0 ERROR** 才可付费生成 |
| 4 生成 | `python s4_generate/gen_one_take.py state/onetake_prompt_job_*.json` | 工作流 `minimax_h3_image_audio_to_video_v2_15s`｜15s｜768p竖｜约 ¥0.9 |
| 5 验收 | 转写（medium）+ 抽帧（ask_img）+ pitch | 台词全出/声画绑定（口型）/'复制人'检查 |
| 6 后期 | `trim_onetake` → SRT → `burn_subtitles --suffix _fx` → `audio_polish --bgm <file> --sfx` | 明快档/动效字幕/剧情BGM |
| 7 归档 | `cp 成片 out/approved/` | 命名 `T{编号}_{标题}_{日期}.mp4` |

**批量出片**：`tools/{brain2_data,prep_brain2,run_batch_brain2,verify_batch_brain2,post_batch_brain2}.py`（数据→组装→批量提交→验收→后期；10 任务并行排队）。

---

## 二、硬性规范（宝哥令·按最新）

### 2.1 影像与产品
- **折叠态必须竖立立在地面上**
- **展开态产品图：折叠 + 正侧 + 45 度三张一起给**（缺一不可）
- **复制人规避**：同框角色服装颜色强对比 + 硬约束 "only ONE xx man, never duplicated"
- **追/跟镜头只露背面**（脸永不出现）；跑动戏写"双人全程可见"
- 运镜要动；"从 X 脸上快速拉到全景"类开场可用

### 2.2 节奏（明快档）
- 开头静默 ≤0.15s｜结尾静默 ≤0.15s｜句间/换人/切镜停顿 ≥0.35s 一律压到 **0.20s**
- `tools/trim_onetake.py --apply` 自动执行；**不裁剪时不输出文件**（后处理须复制原片兜底）

### 2.3 台词
- **15s 档：68-75 汉字**（check_dialogue.mjs 口径 = 纯 CJK 字数）
- 每镜 2-3 句、镜内双人来回；单镜 ≤3 人；全片 2 人为主
- **禁破折号 "——"**（含破折号的整句会被 H3 丢弃，两次实测）
- 标点只用 `，。？！`；数字写中文（"三十九公里"）
- 字幕文本会经 ASS 层自动去标点（见 2.5）

### 2.4 提示词（官方 Ref2VA 六段式 · 2026-09-25 起）
- **六段固定顺序**：`subject_definitions` → `summary` → `retention_analysis` → `detailed_description` → `overall_soundscape` → `non_diegetic_music`
- **音色-主体绑定显式声明**：`<Audio 1> is the voice-timbre reference for <Subject 1> (S1).`（降"说话人错配"）
- **切点格式**：`[Shot 2] At 00:04.000, the shot cuts to ...`（**Shot 1 不加时间戳**）
- **相机运动 = 类型+幅度+速度** 自然句（如 The camera pulls out with small amplitude at fast speed ...）
- 可见画面文字用英文双引号 + 原文
- 完整规范：`skills/media/autodl/references/prompt-writing.md`（含英文原文）

### 2.5 字幕（2026-09-25 最新）
- **字体：新青年体**（文悦新青年体 非商用 W8；fontsdir=`D:/@kaifa/fonts-douyin`）
- **不加粗**（ASS Style Bold=0）
- **不能有任何标点**（ASS 层 `_strip_punct` 自动清洗；中文数字转阿拉伯）
- 关键词黄字高亮 + 出场弹跳动效
- 底部位置：0.25 比例（=底边距 25.5%）

### 2.6 BGM 与音效
- **BGM 从 `assets/bgm_trending/`（36 首抖音热门）按剧情逐条分配、零重复**（分配表模板见 `docs/bgm分配-脑洞10连第二批-20260925.json`）
- `audio_polish.py --bgm "assets/bgm_trending/xx.mp3" --sfx --transcripts <json>`
- 音效 3 种：ding/whoosh/pop，按台词关键词自动插入

### 2.7 音色
- 真人样本库：`assets/cast/voice/real/`（42 个）+ 映射表 `docs/音色-角色映射表-20260925.md`
- **标准普通话样本优先**；口音样本只配"特色老外"角色

---

## 三、现行脚本清单（26 个 tools + 4 个 s4）

| 脚本 | 用途 |
|---|---|
| `tools/pick_combo.py` | 四池智能组合 |
| `tools/check_dialogue.mjs` | prompt 校验（0 ERROR 门槛） |
| `tools/check-take.mjs` | 出片后逐字回读校验 |
| `s4_generate/gen_one_take.py` | 单条生成（提交→轮询→下载） |
| `s4_generate/autodl_client.py` | API 底座（submit/query/download） |
| `s4_generate/workflow_router.py` | 工作流路由 |
| `tools/trim_onetake.py` | 明快档裁剪 |
| `tools/transcribe_local.py` | 本地转写 |
| `tools/ask_img.py` | 识图（--ch deepseek/local） |
| `tools/frames_card.py` | 抽帧对比卡 |
| `s5_compose/burn_subtitles.py` | 烧字幕（新青年体/无标点/不加粗/动效） |
| `tools/audio_polish.py` | BGM+音效后期 |
| `tools/prep_w1_magic.py` | **spec 组装最新样板**（Ref2VA 六段式） |
| `tools/prep_t20_v3.py` | T20 最终版 spec 样板 |
| `tools/brain2_data.py` | B2 10连结构化数据 |
| `tools/prep_brain2.py` | B2 spec 批量组装 |
| `tools/run_batch_brain2.py` | 批量提交+轮询下载 |
| `tools/verify_batch_brain2.py` | 批量转写验收 |
| `tools/post_batch_brain2.py` | 批量后期（trim→字幕→BGM→归档） |
| `tools/make_voice_sample.py` | 长音频→样本切片 |
| `tools/podcast_voice_harvest.py` | 播客采集切样本 |
| `tools/build_real_voice_library.py` | 审核带→样本库 |
| `tools/update_real_index.py` | 样本库 INDEX 更新 |
| `tools/gen_sfx.py` | 合成音效（ding/whoosh/pop） |
| `tools/make_review_card.py` | 审核预览卡 |
| `tools/bootstrap.py` / `cleanup_project.py` | 部署/清理 |
| `tools/fetch_bridges.py` / `fetch_trends_multi.py` | 桥段库/热点库 |
| `tools/retime.mjs` | 语速归一化 |

---

## 四、已弃用（`_deprecated_20260925/`，**勿用**）

| 组 | 内容 | 弃用原因 |
|---|---|---|
| 模板链路 | `run_all.py`、`gen_from_storyboard.py`、`batch_gen.py`、`s3_storyboard/`、`merge.py`、`subtitle.py`、`tts.py` | 已被 one-take 单条多镜取代（模板停用） |
| 历史 prep | `prep_t11.py`~`prep_t20.py`、`prep_t20_v2.py` | 一次性脚本；新稿参照 `prep_w1_magic.py`/`prep_t20_v3.py` |
| 一次性建库 | `gen_cast*.py`×6、`gen_fashion_*.py`×2、`gen_voice_cast*.py`×2、`gen_pixabay_cast.py`、`gen_realistic_faces.py`、`gen_all_realistic.py`、`_add_roles_*.py`、`_append_web_bridges.py`、`_fix_t07_v4.py`、`build_audit_tape_v2/v3.py` | 资产已就绪（95图/42音色），不再需要 |
| 测试/探测 | `test_*.py`×16、`probe_multi_audio*.py`、`check_dual_voice.py`、`verify_voice_clone.py`、`voice_match.py`、`verify_take.py`、`compare_side_by_side.py` | 一次性验证脚本 |
| 重复文件 | `check-dialogue.mjs` | 与 `check_dialogue.mjs` 完全相同（保留后者） |

---

## 五、关键资产路径

| 资产 | 路径 |
|---|---|
| 角色图（95 张照片级） | `assets/cast/library/`（+ roles.yaml 100 人） |
| 音色库（42 样本） | `assets/cast/voice/real/`（+ INDEX.md） |
| 产品图（三视图） | `assets/products/{折叠,正侧-3,45度-加水杯}-无阴影.png` |
| BGM 库（36 首抖音热门） | `assets/bgm_trending/`（+ INDEX.md 选曲建议） |
| 音效 | `assets/sfx/{ding,whoosh,pop}.mp3` |
| 字体（抖音全套） | `D:/@kaifa/fonts-douyin/`（新青年体等 10+ 款） |
| 四池数据 | `lib/{products,angles,genres}.py` + `assets/cast/library/roles.yaml` |
| 成片归档 | `out/approved/` |
