# 成片归档与中间文件清理记录（2026-09-25）

宝哥审核完桌面 `C:\Users\xxx13\Desktop\ayh-mj` 的 36 部成片后，清理全部生产过程中间文件。

## 保留（成片 + 资产）

| 位置 | 内容 | 数量/大小 |
|---|---|---|
| 桌面 `ayh-mj\` | 已审核成片 mp4 | 36 部 / 223 MB |
| `out/approved/` | 定稿成片 mp4 | 36 部 / 223 MB |
| `out/gen_job_*/onetake.mp4` | AI 生成原片（保留备重剪，**待宝哥确认是否删除**） | 17 条 / 228 MB |
| `assets/` | 素材库：bgm_trending 89M / cast 160M / products 42M / scripts | 291 MB |
| `vendor/postflow` | 发布工具链 | 276 MB |
| `state/browser-profile/` | 浏览器登录态 | 59 MB |
| `state/*_used.json` `*_history.json` `cast_*.json` | 去重库 + 选角记录 | 数 KB |
| `state/_tmp_10lian.json` `batch_brain2_*.json` | 短剧狗血10连待跑数据 + 10连批次记录 | 数 KB |
| `state/onetake_prompt_job_D1.json` | D1（菜场试坐）任务记录 | 7 KB |
| `docs/*.md` `docs/research/` | 脚本、规范、执行标准、调研资料 | 806 KB |
| `_deprecated_20260925/` | 老管线归档（保留） | 465 KB |

## 已删除（中间产物，约 540 MB）

| 类别 | 明细 |
|---|---|
| 桌面中间文件 | storyboard_*.json ×17、onetake_prompt_job_*.json ×2、onetake_trim_sub.mp4、docs 副本 md |
| `out/gen_job_*/` | 裁剪版/加效版/polished 版 mp4、.ass/.srt 字幕、chk 校验帧 jpg、transcripts 目录（14 个目录共 470 MB） |
| `out/approved/` | storyboard json ×17、prompt job json ×2、误入的 onetake_trim_sub.mp4 |
| `state/` | combo_*.json ×10、storyboard_job_*.json ×15、onetake_prompt_job_*.json ×29（除 D1）、`state/frames/`（27 M 抽帧）、`state/thumbs/` |
| `docs/` | 抽帧校验图/审核卡片/控制台截图 PNG（43 M）、onetake_check_*.txt 质检文本 |
| `logs/` | 全部 *.log（46 个） |

## 遗留

- `state/logs/`：**已删除**（2026-09-25 18:1x）。删除前需先停掉 webui 拉起的孤儿后端 `hermes serve --skip-build --port 9277`（PID 16440 链），该进程持有 `hermes_serve.log` 写句柄；已停。
- webui 主服务 `http://127.0.0.1:8899`（爱优护AI视频工厂·控制台）仍在监听，未动；下次使用会自动重新拉起 9277 后端并重建 `state/logs/hermes_serve.log`（属正常运行时日志）。
- `out/gen_job_*` 目前只剩 `onetake.mp4`（17 条，228 MB）——如需彻底清空请确认。
- 新批次数据保留：`state/_tmp_10lian.json`、`state/_tmp_10lian_v2.json`、`state/_shot_lines.json`（短剧狗血10连待跑）。
- 2026-09-25 晚追加：删除 `%LOCALAPPDATA%\Temp\trim_onetake_*` 16 个 ffmpeg 临时残留目录（148 MB，均为 09-25 生成）。**17 条生片 `out/gen_job_*/onetake.mp4`（228 MB）按宝哥指示保留。**
